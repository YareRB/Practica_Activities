<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DescriptionFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clcDescription"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activity_description"
        android:layout_weight="1">

        <ImageView
            android:id="@+id/imgObjectD"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:layout_marginTop="4dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.496"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/cerdo" />

        <ImageView
            android:id="@+id/imgFavorite"
            android:layout_width="74dp"
            android:layout_height="65dp"
            android:src="@drawable/ic_image_no_favorite"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.237"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/txvDescription"
            app:layout_constraintVertical_bias="0.05" />

        <TextView
            android:id="@+id/txvDescription"
            android:layout_width="298dp"
            android:layout_height="64dp"
            android:layout_marginTop="28dp"
            android:text="@string/text_description"
            android:textAlignment="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.495"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/imgObjectD" />

        <ImageView
            android:id="@+id/soundFavorite"
            android:layout_width="73dp"
            android:layout_height="64dp"
            android:layout_marginEnd="88dp"
            android:src="@drawable/ic_sound_no_favorite"
            app:layout_constraintBottom_toBottomOf="@+id/imgFavorite"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/imgFavorite"
            app:layout_constraintVertical_bias="1.0" />

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="17dp"
            android:layout_marginEnd="13dp"
            android:text="@string/text_sonido"
            app:layout_constraintBottom_toTopOf="@+id/soundFavorite"
            app:layout_constraintEnd_toEndOf="@+id/soundFavorite"
            app:layout_constraintHorizontal_bias="1.0"
            app:layout_constraintStart_toStartOf="@+id/soundFavorite" />

        <TextView
            android:id="@+id/textView2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/txt_Image"
            app:layout_constraintBottom_toTopOf="@+id/imgFavorite"
            app:layout_constraintEnd_toEndOf="@+id/imgFavorite"
            app:layout_constraintStart_toStartOf="@+id/imgFavorite" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>